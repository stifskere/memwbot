services:
  memwbot:
    container_name: memwbot
    build:
      dockerfile: dev.dockerfile
    ports:
      - 8080:8080
    volumes:
      - ../:/home/dev/memwbot
      - memwbot-cargo-target:/home/dev/memwbot/target

volumes:
  memwbot-cargo-target:
